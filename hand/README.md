# Required handwritten evidence — still pending

There is no authentic handwritten photograph in this repository yet. A typeset derivation, generated handwriting, or a photo placeholder does **not** satisfy the assignment.

Work through the following yourself on paper, photograph it legibly, and save your real photo as `hand/derivation.jpg` (an actual JPEG). Both decks automatically include this file when recompiled. Keep the page in landscape orientation if possible.

1. Define `Y = σ⁻² + λ_I e + τ_A` and `G(z) = 2Φ(√z) − 1`.
2. Differentiate to obtain `g(z) = φ(√z)/√z` and `g′(z) = −(1 + 1/z)g(z)/2`.
3. Write the static FOC and the two cross-partials; label what is held fixed and the strict-sign conditions `X > 0`, `Y > 0`, `Δ_X > 0`, `λ_I > 0`.
4. Add `Δ_I G(Y)` to utility and compute `U_e` at `X = e = 0`. Explain why it is strictly positive for `Δ_I > 0` and finite AI precision.
5. Add your own verdict: the signs survive, but the zero-effort boundary relies on `Δ_I = 0`. Fixed-X utility is not long-run welfare.

The worked reference is in `extra/analysis.md`; use it to check your work, not to claim that a derivation was independently completed before it was.
